[project]
name = "voice-recognize-system-prototype"
version = "0.3.0"
description = "Phase 1 cloud streaming ASR demo"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.1",
    "python-multipart>=0.0.9",
    "pyyaml>=6.0.1",
    "uvicorn>=0.30.0",
    "websockets>=12.0",
]

[tool.uv]
package = false
